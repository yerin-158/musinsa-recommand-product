export interface ProductByCategoryResponse {
  // Define the structure based on your API response
}

export interface ProductSetResponse {
  // Define the structure based on your API response
}

export interface PriceType {
  // Define the structure based on your API request
}
